kind: pipeline
type: docker
name: default

steps:
  - name: build
    image: golang
    commands:
      - go test ./...
      - go build -ldflags "-s -w -X main.version=`git describe --tags` -X main.commit=`git describe --always` -X main.date=`date -u +%FT%TZ`" -o dronetest ./main.go
